// That's the index where all shared components should go
// For now only the Temprature Control we have
import TemperatureControl from "./TempratureSlider/TempratureSlider";

export {TemperatureControl}