import { StyleSheet } from "react-native";

export default StyleSheet.create({
    slider: {
      margin: 10,
      flex: 1,
      height: 40,
    },
    wrapper: {
      flexDirection:'row',
      justifyContent: 'center',
      alignItems: 'center',
      padding: 16,
    },
  });
  